model Candidate {
  id                Int       @id @default(autoincrement())
  name              String
  email             String    @unique
  password          String
  phone             String
  age               Int
  info              Json?
  onboardingInfo    Json?
}